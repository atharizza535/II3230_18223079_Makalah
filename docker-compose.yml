services:
  replay-protection:
    build: .
    command: npm run ci
    environment:
      DEMO_CLIENT_ID: demo-client
      DEMO_CLIENT_SECRET: demo-secret-do-not-use-in-production
      CORPUS_VERSION: 2026-06-v1
      TIME_BUCKET_SECONDS: "30"
      MAX_CLOCK_SKEW_SECONDS: "60"
